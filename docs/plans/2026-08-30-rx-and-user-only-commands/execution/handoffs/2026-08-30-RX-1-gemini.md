# Handoff — RX-1 — Manifest userOnly Field (Gemini)

## Goal
Add optional per-command `userOnly?: boolean` to the RunX manifest. This is INFORMATIONAL only — no execution guard, no refusal code. If an agent runs a userOnly command, it still runs. The skill will teach agents to warn the user.

## Context
- Project: C:/GUIHO/runx — Go/Cobra CLI, strict YAML manifest v2
- Branch: feat/rx-and-user-only-commands (from 805a1be)
- Brainstorm: docs/brainstorm/2026-08-30-rx-short-alias-and-user-only-commands.md
- Requirements: docs/requirements/2026-08-30-rx-and-user-only-commands.md (UO-F1, UO-F6)
- Architecture: docs/architecture/2026-08-30-rx-and-user-only-commands.md ADR-004 (UserOnly *bool leaf-only)
- Plan: docs/plans/2026-08-30-rx-and-user-only-commands.md — RX-1 only

## Skills to Load
- Read guiho-s-mandume for Mandume context (phase 2 execution, no secrets)
- Read guiho-s-0016-docs for frontmatter/copyright if you touch docs (you won't)
- Follow project AGENTS.md mandatory CLI convention (read C:/GUIHO/guiho/conventions/guiho-convention-0001-cli.md if needed, but not required for this small schema change)

## Constraints
- **ONLY** touch: pkg/manifest/types.go, pkg/manifest/parser.go, pkg/manifest/composition.go
- Do NOT touch: pkg/manifest/manifest_test.go (unless you add a tiny test), schemas/runx.schema.json (config schema, not manifest), cmd/*, pkg/executor, installers, skills, XDocs
- **No enforcement code** — do not add any refusal, exit code, or executor guard. Field is informational.
- Keep strict YAML (yaml.KnownFields(true)) — unknown fields must still fail
- Use full workspace permissions via --dangerously-skip-permissions

## Detailed Tasks
1. In pkg/manifest/types.go:
   - Command: add `UserOnly *bool `yaml:"userOnly,omitempty" json:"userOnly,omitempty"``
   - ResolvedCommand: add `UserOnly bool `json:"userOnly"``
2. In pkg/manifest/parser.go — validateEntries:
   - For group entries (entry.Group != ""): if entry.UserOnly != nil return error `group %q cannot declare userOnly`
   - Leaf entries: no extra validation (bool pointer, YAML will enforce boolean)
3. In pkg/manifest/composition.go — registerCommand:
   - Compute userOnly := false; if entry.UserOnly != nil { userOnly = *entry.UserOnly }
   - Pass UserOnly: userOnly into ResolvedCommand initialization
4. Validate:
   - go vet ./pkg/manifest/...
   - go test -count=1 ./pkg/manifest/...
   - Manual: ParseManifestBytes with leaf userOnly:true (should parse), omitted (should be false), group with userOnly (should error)

## Output Contract
- Return: list of changed files, git diff --stat, go vet and go test output, and confirmation that leaf true/omitted/group-error behave correctly
- Do NOT commit or push — orchestrator will commit after human validation
- Do NOT modify docs/questions — orchestrator handles ledger

## Worker
- Harness: agy
- Provider: google
- Model: gemini-3.7-flash-high
- Effort: high
