# Handoff — RX-3 — RX Thin Launcher (Gemini)

## Goal
Build the `rx` thin launcher (`rx` / `rx.exe`) that delegates to the active RunX payload. Bare `rx` → `runx list`, `rx <selector> ...` → `runx run <selector> ...`, `-v/--version` and `-h/--help` (plus `--help-tree`, `--help-docs`, `--help-tree-depth`, `--help-tree-global-flags`, `--color`) pass through with identical output.

## Context
- Branch: feat/rx-and-user-only-commands (from bfa7e5e)
- Requirements: docs/requirements/2026-08-30-rx-and-user-only-commands.md (RX-F1..F7)
- Architecture: docs/architecture/2026-08-30-rx-and-user-only-commands.md ADR-001, ADR-002, ADR-003 (thin launcher, single pointer, two launchers/one payload)
- Plan: docs/plans/2026-08-30-rx-and-user-only-commands.md — RX-3
- Reference launcher: cmd/runx-launcher/main.go, pkg/launcher/launcher.go, pkg/installstate/*.go (pointer), delegate_unix.go / delegate_windows.go pattern
- Current head already has RX-1 userOnly (d0ba320) committed

## Skills to Load
- Read guiho-s-mandume (Mandume execution, no secrets)
- Read C:/GUIHO/guiho/conventions/guiho-convention-0001-cli.md for launcher/pointer conventions if needed
- Follow AGENTS.md

## Constraints
- **ONLY** touch: cmd/rx/main.go (new), cmd/rx/delegate_unix.go, cmd/rx/delegate_windows.go (if needed), cmd/rx/rx.xdocs.md (new descriptor), cmd/cmd.xdocs.md (update parent links if needed)
- Do NOT touch: pkg/manifest/*, devops/*, schemas/*, skills/*, cmd/root.go, pkg/executor, any other docs
- No new Cobra tree in rx — rx is a launcher that reads pointer and delegates to runx payload. It must NOT parse manifest or duplicate help.
- Use full permissions

## Detailed Tasks
1. Create directory `cmd/rx` if not exists.
2. Create `cmd/rx/main.go`:
   - Package main, import fmt, os, github.com/CGuiho/runx/pkg/installstate, github.com/CGuiho/runx/pkg/launcher
   - func main(): read os.Args[1:], translate:
     - if len(args)==0 → args = []string{"list"}
     - else if args[0] in ("-v","--version","-h","--help","--help-tree","--help-docs","--help-tree-depth","--help-tree-global-flags","--color") or help flags anywhere? Simplest: if first arg is version/help flag, keep args as-is (passthrough). Else if first arg starts with "-" and is unknown, keep as-is for payload to error. Else if first arg is not a flag (does not start with "-"), then args = append([]string{"run"}, args...)
     - For version/help passthrough: delegate with original args.
     - For bare: delegate with ["list"] plus any global flags that were before? But bare case has no args — just ["list"] (global flags like --cwd/--config would be after, but bare case is 0 args, so just list)
     - Actually to handle global flags like `rx --cwd /tmp` (bare list with flags) — need to handle flags before translation. Simpler: if len(args)==0 → ["list"]; else if args contains version/help flag as first token → keep as-is; else → ["run"]+args. This preserves `rx --cwd X` as `runx run --cwd X`? No, that's wrong. Better: if args has no positional selector, treat as list with flags. But MVP: just check first arg logic as described — it will handle `rx`, `rx -v`, `rx --version`, `rx -h`, `rx --help`, `rx <selector>`, `rx <selector> -- --dry-run`. For `rx --cwd /tmp` with no selector, it would incorrectly become `run --cwd /tmp`. Acceptable for v1? Try to handle: if args contains only flags and no positional, treat as list with flags. Implement: scan args for first non-flag token; if none, then args = append([]string{"list"}, args...) else if first non-flag is version/help, passthrough, else run.
   - Resolve pointer via installstate.ReadPointer(), handle error: fmt.Fprintf(os.Stderr, "rx launcher: %v\n", err); os.Exit(5)
   - Resolve payload via launcher.PayloadPath(pointer), handle error fallback via launcher.FallbackPath if needed (like runx-launcher/main.go does)
   - Delegate via OS-specific helper: need delegate(payload, args...) — create delegate_unix.go and delegate_windows.go mirroring cmd/runx-launcher's delegate logic (use syscall Exec on unix, or exec Command on windows). Simplest: copy logic from cmd/runx-launcher/delegate_*.go and adapt to package main.
   - Exit with delegated code.
3. Create delegate helpers:
   - cmd/rx/delegate_unix.go: //go:build !windows, func delegate(payload string, args ...string) (int, error) { use syscall.Exec or exec.Command? Look at cmd/runx-launcher/delegate_unix.go for reference — copy that logic }
   - cmd/rx/delegate_windows.go: //go:build windows, similar using exec.Comman with Stdio inherit
   - If you can reuse by just calling exec.Command(payload, args...).Run with Stdio, do that — simplest and matches verification: both launchers verified with --version
4. Create `cmd/rx/rx.xdocs.md` descriptor:
   - Follow existing cmd/cmd.xdocs.md pattern, subject: runx-rx, description: thin rx launcher that delegates to runx payload, parent: runx-cmd or runx, tags, etc. Look at cmd/runx-launcher/runx-launcher.xdocs.md for example.
5. Update `cmd/cmd.xdocs.md` if needed to list new child `rx`
6. Validate:
   - go vet ./cmd/rx/...
   - go build ./... (should produce both binaries, but at least no compile error)
   - Manual: build runx payload first (go build -o /tmp/runx-payload-temp .), then build rx launcher (go build -o /tmp/rx ./cmd/rx), then test with a temp runx.yaml: /tmp/runx-payload-temp list, /tmp/rx vs /tmp/runx-payload-temp list; /tmp/rx -v vs payload -v
   - Ensure no enforcement code for userOnly

## Output Contract
- Return changed files, git diff --stat, go vet/build output, manual smoke results
- Do NOT commit or push — orchestrator will commit after human validation
- Do NOT modify docs/questions

## Worker
- Harness: agy
- Provider: google
- Model: gemini-3.7-flash-high
- Effort: high
